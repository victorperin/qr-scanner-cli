on:
  workflow_run:
    workflows: ["Check"]
    branches: [master]
    types:
      - completed

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print success
        run: echo "success! :)"
