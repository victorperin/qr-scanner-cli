on:
  workflow_run:
    workflows: ["pr-check"]
    branches: [master]
    types:
      - completed

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print success
        run: echo "success! :)"
